{
  "name": "a9n",
  "version": "0.1.0",
  "description": "AI-agnostic workflow orchestration engine, exposed as a Claude Code plugin with a single MCP tool. A port of Claude Code's Workflow tool whose agent() primitive hits any OpenAI-compatible LLM provider.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test",
    "smoke": "bun run scripts/smoke-server.ts",
    "typecheck": "bunx tsc --noEmit",
    "bump": "bun run scripts/bump.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
